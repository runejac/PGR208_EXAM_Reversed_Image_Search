package com.example.eksamen_pgr208.utils

// the shell of the callback
interface AnimationCallback {
    fun setAnimation(fabClicked: Boolean) {
    }

    fun setVisibility(fabClicked: Boolean) {
    }
}